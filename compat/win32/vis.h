#ifndef _COMPAT_WIN32_VIS_H_
#define _COMPAT_WIN32_VIS_H_
#include "../win32.h"
/* vis.h specific definitions are in tmux-port.h or win32.h now? 
   Actually, tmux has its own compat/vis.h, but we want to make sure 
   it's found if included as <vis.h>. */
#endif
